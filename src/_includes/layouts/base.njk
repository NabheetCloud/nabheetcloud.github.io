<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  
  <title>{% if title %}{{ title }} | {% endif %}Tech Learnings</title>
  
  {# Basic Meta tags #}
  <meta name="description" content="{{ description or 'Deep engineering, AI, and system-architecture insights' }}">
  <meta name="author" content="{{ author.name }}">
  
  {# T068: OpenGraph Meta Tags #}
  <meta property="og:title" content="{% if title %}{{ title }} | {% endif %}Tech Learnings">
  <meta property="og:description" content="{{ description or excerpt or 'Deep engineering, AI, and system-architecture insights' }}">
  <meta property="og:type" content="{% if layout == 'layouts/post.njk' %}article{% else %}website{% endif %}">
  <meta property="og:url" content="{{ page.url }}">
  {% if featured_image %}
  <meta property="og:image" content="{{ featured_image }}">
  <meta property="og:image:alt" content="{{ featured_image_alt or title }}">
  {% else %}
  <meta property="og:image" content="/assets/images/og-default.jpg">
  <meta property="og:image:alt" content="Tech Learnings - Engineering Insights">
  {% endif %}
  <meta property="og:site_name" content="Tech Learnings">
  
  {# T069: Twitter Card Meta Tags #}
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{% if title %}{{ title }} | {% endif %}Tech Learnings">
  <meta name="twitter:description" content="{{ description or excerpt or 'Deep engineering, AI, and system-architecture insights' }}">
  {% if featured_image %}
  <meta name="twitter:image" content="{{ featured_image }}">
  <meta name="twitter:image:alt" content="{{ featured_image_alt or title }}">
  {% else %}
  <meta name="twitter:image" content="/assets/images/og-default.jpg">
  <meta name="twitter:image:alt" content="Tech Learnings - Engineering Insights">
  {% endif %}
  {% if author.social.twitter %}
  <meta name="twitter:site" content="@{{ author.social.twitter }}">
  <meta name="twitter:creator" content="@{{ author.social.twitter }}">
  {% endif %}
  
  {# T074: Preconnect to Google Fonts #}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

  {# FIXED CSS PATHS — use actual build output #}
  <link rel="stylesheet" href="/assets/styles/main.css">
<link rel="stylesheet" href="/assets/styles/prism-theme.css">


  {# Theme detection #}
  <script>
    (function() {
      const theme = localStorage.getItem('theme') || 
        (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'dark');
      if (theme === 'dark') {
        document.documentElement.classList.add('dark');
      }
    })();
  </script>

  {% block head %}{% endblock %}
</head>

<body class="bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 transition-colors duration-300">
  
  <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-accent-light focus:text-white focus:rounded-lg">
    Skip to main content
  </a>
  
  {% if layout == "layouts/post.njk" %}
    {% include "partials/reading-progress.njk" %}
  {% endif %}

  {% include "partials/header.njk" %}
  
  <main id="main-content">
    {{ content | safe }}
  </main>
  
  {% include "partials/footer.njk" %}
  
  <script src="/assets/js/theme-toggle.js" defer></script>
  {% block scripts %}{% endblock %}
</body>
</html>
