---
layout: layouts/base.njk
title: Archive
description: Complete archive of all blog posts organized by year
permalink: /archive/
---

<div class="max-w-4xl mx-auto px-4 py-8 md:py-12">
  <header class="mb-12">
    <h1 class="text-4xl md:text-5xl font-bold mb-4 text-gray-900 dark:text-gray-100">
      Archive
    </h1>
    <p class="text-lg text-gray-600 dark:text-gray-400">
      All {{ collections.posts.length }} posts
    </p>
  </header>

  {# Simple chronological list for now - can be enhanced later #}
  <div class="space-y-8">
    {% for post in collections.posts %}
    <article class="flex gap-6 items-start group pb-6 border-b border-gray-200 dark:border-gray-800 last:border-b-0">
      <time datetime="{{ post.date }}" class="text-sm font-mono text-gray-500 dark:text-gray-500 mt-1 w-32 flex-shrink-0">
        {{ post.date | dateFormat }}
      </time>
      
      <div class="flex-1">
        <a href="{{ post.url }}" class="block mb-2">
          <h3 class="text-xl font-semibold text-gray-900 dark:text-gray-100 group-hover:text-accent-light dark:group-hover:text-accent-dark transition-colors">
            {{ post.data.title }}
          </h3>
        </a>
        
        {% if post.data.excerpt %}
        <p class="text-gray-600 dark:text-gray-400 text-sm mb-3">
          {{ post.data.excerpt }}
        </p>
        {% endif %}
        
        {% if post.data.tags %}
        <div class="flex flex-wrap gap-2">
          {% for tag in post.data.tags %}
          <a href="/tags/{{ tag | slug }}/" class="text-xs px-2 py-1 rounded-full bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 hover:bg-accent-light hover:text-white dark:hover:bg-accent-dark transition-colors duration-200">
            {{ tag }}
          </a>
          {% endfor %}
        </div>
        {% endif %}
      </div>
    </article>
    {% endfor %}
  </div>
</div>

